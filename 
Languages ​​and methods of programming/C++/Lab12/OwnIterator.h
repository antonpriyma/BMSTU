//
// Created by anton on 25.05.18.
//

#ifndef LAB12_OWNITERATOR_H
#define LAB12_OWNITERATOR_H

#include <iterator>
#include <string>

/*
class OwnIterator:public ::std::iterator<std::forward_iterator_tag,char>{
    friend class OwnContainer;
private:
    char* p;
public:
    OwnIterator(string);
    bool operator!=(OwnIterator const& other) const;
    bool operator==(OwnIterator const& other) const;
    OwnIterator& operator++();

    OwnIterator(std::string basic_string);

    OwnIterator();
};
 */

#endif //LAB12_OWNITERATOR_H
